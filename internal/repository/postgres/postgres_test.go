package postgres

import (
	"testing"
)

func TestConfig(t *testing.T) {
	//repo := New("")
	//repo.db.
	file := `repository:
				type: postgres
				url:
				testUrl:`
	//mock_repository.NewMockRepository(&gomock.Controller{})
	t.Skip()
}
