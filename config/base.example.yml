api:
  port: 8081
  host: localhost
  version: "1.23"

services:
  auth:
    jwt:
      accessKey: ...
      refreshKey: ...
      issuer: okapi
    oauth:
      google:
        clientID: ...
        clientSecret: ...
        redirectURL: http://localhost:8081/auth/google/redirect
        scopes:
          - https://www.googleapis.com/auth/userinfo.email
          - https://www.googleapis.com/auth/userinfo.profile

repository:
  type: dev
  url: ...
